== README

Sistema gera consultas definidas com o setor de rh referente a tabela pessoas
Ele pode tanto apresentar na tela como gerar um .xls

Para fazer uso
Colocar no database.yml o usuário e senha do replicado da unidade

bundle install
rails s


